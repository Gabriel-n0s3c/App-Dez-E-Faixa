import 'package:flutter/material.dart';

class PageDefinirTimes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Definir times"),
      ),
    );
  }
}
